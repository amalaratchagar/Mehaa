@using Core.Entities.Inventory
@model IEnumerable<ItemCategory>
<table class="table table-bordered" id="itemCategoryTable">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Edit
            </th>
            <th>
                Remove
            </th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() != 0)
        {
            @foreach (var itemCategory in Model)
            {
        <tr>
            <td>
                @itemCategory.Name
            </td>
            <td text-right">
                <a onclick="jQueryModalGet('?handler=CreateOrEdit&id=@itemCategory.Id','Edit Customer')" class="btn btn-info text-white"> <i class="fa fa-edit"></i></a>
            </td>
            <td text-right">
                <form method="post" asp-page="Index" asp-route-id="@itemCategory.Id" asp-page-handler="Delete" onsubmit="return jQueryModalDelete(this)" class="d-inline">
                    <button type="submit" class="btn btn-danger text-white"> <i class="fa fa-trash"></i></button>
                </form>
            </td>
        </tr>
            }
        }
    </tbody>
</table>
<script>
    $(document).ready(function () {
        $("#itemCategoryTable").DataTable();
    });
</script>